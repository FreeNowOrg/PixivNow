export enum IllustType {
  ILLUST = 0,
  MANGA = 1,
  UGOIRA = 2,
}

export enum UserXRestrict {
  SAFE,
  R18,
  R18G,
}

export enum UserPrivacyLevel {
  PUBLIC_FOR_ALL,
  PUBLIC_FOR_FRIENDS,
  PRIVATE,
}
